# 2026-04-24 日志

## 调酒软件项目进展

### 完成
- 修复 JS 语法错误（Sotheby's 撇号导致字符串截断）
- 修复导航点击失效问题（改用 data-page + addEventListener）
- Electron 桌面应用搭建完成：
  - `main.js` - 主进程（含 IPC 数据持久化、CSV 导出）
  - `preload.js` - 安全桥接层
  - `package.json` - electron-builder 配置
  - 成功打包：`dist/win-unpacked/BarCraft.exe`（免安装便携版）

### 重要路径
- 项目根目录：`c:/Users/otisfeng/OneDrive/桌面/workbuddy/调酒软件/`
- Electron 入口：`main.js`
- preload：`preload.js`
- 打包产物：`dist/win-unpacked/BarCraft.exe`
- 数据存储：`%APPDATA%/BarCraft/data/`（recipes.json, cellar.json, inventory.json）

### 待完成
- 生成应用图标（assets/icon.ico）
- 完善 NSIS 安装包（需要图标文件）
- 数据持久化接入（配方、库存、酒柜数据保存到本地 JSON）
- CSV 导出功能接入前端
